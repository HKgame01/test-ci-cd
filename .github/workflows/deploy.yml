name: Deploy

on:
  push:
    branches: [dev]

jobs:
  Deploy:
    name: Deploy to EC2
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Build & Deploy
        run: |
          echo >> "${{ secrets.SSH_KEY }}" > private_key && chmod 600 private_key
          ssh -i private_key ubuntu@ec2-13-51-193-53.eu-north-1.compute.amazonaws.com
          ls
